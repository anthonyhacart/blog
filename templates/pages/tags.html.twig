{% extends 'base.html.twig' %}

{% block body %}
    <section class="text-gray-400 bg-gray-900 body-font">
        <div class="container px-5 py-24 mx-auto">
            <div class="flex flex-wrap -m-4">
                {% for item in contents %}
                    <twig:Content:ItemListing content="{{ item }}"/>
                {% endfor %}
            </div>
        </div>
    </section>
{% endblock %}
